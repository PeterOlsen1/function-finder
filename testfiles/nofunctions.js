console.log("awkward!");


//i had to write so many of these for work
window.addEventListener('keydown', (evnet) => {
    if (event.key === 'Enter') {
        SubmitEvent();
    }
})

//function do_stuff(don't parse this!)

'function dont_parse_this_either(a, b, c, d, e, f)';
"function no_parsing(x, y, z)";

export function hello() {
    console.log('hello')
}

hello();